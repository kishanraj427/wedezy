import { NotFound } from '@/components/layout/NotFound'

export function CollectionNotFound() {
  return (
    <NotFound
      title="Collection not found"
      description="That collection is no longer available. Browse all venues instead."
    />
  )
}
