import { NotFound } from '@/components/layout/NotFound'

export function VenueNotFound() {
  return (
    <NotFound
      title="Venue not found"
      description="This listing may have been removed. Browse the full catalogue instead."
    />
  )
}
